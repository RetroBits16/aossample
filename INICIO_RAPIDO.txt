╔══════════════════════════════════════════════════════════════╗
║  🛒 SISTEMA DE GESTIÓN DE SUPERMERCADO - INICIO RÁPIDO      ║
╚══════════════════════════════════════════════════════════════╝

📋 REQUISITOS:
   • Python 3.7 o superior
   • pip (gestor de paquetes de Python)

🚀 INICIO RÁPIDO (3 PASOS):

   1️⃣  Instalar dependencias:
      
      pip install -r requirements.txt

   2️⃣  Iniciar el servidor backend:
      
      Linux/Mac:    ./start.sh
      Windows:      start.bat
      
      O manualmente: python main.py

   3️⃣  Abrir el frontend:
      
      Abre el archivo: frontend/index.html en tu navegador
      
      O con servidor local:
      cd frontend
      python -m http.server 8080
      Luego abre: http://localhost:8080

📊 ¿QUÉ INCLUYE?

   ✅ Base de datos SQLite con 20 productos de ejemplo
   ✅ API REST completa (ver http://127.0.0.1:8000/docs)
   ✅ Interfaz web moderna y responsiva
   ✅ CRUD completo de productos
   ✅ Categorías y descripciones
   ✅ Operaciones adicionales

🔗 URLs IMPORTANTES:

   Backend API:          http://127.0.0.1:8000
   Documentación API:    http://127.0.0.1:8000/docs
   Frontend:             frontend/index.html

📁 ESTRUCTURA:

   supermercado_app/
   ├── app/                  → Backend (Python/FastAPI)
   │   ├── database/         → Base de datos SQLite
   │   ├── models/           → Modelos Pydantic
   │   └── routes/           → Endpoints API
   ├── frontend/             → Frontend (HTML/CSS/JS)
   └── main.py               → Punto de entrada

🎯 PRIMEROS PASOS:

   1. Verifica que el backend esté corriendo (debe mostrar productos)
   2. Abre el frontend y haz clic en "Actualizar"
   3. Prueba agregar un nuevo producto
   4. Explora las operaciones en la pestaña "Operaciones"

🐛 PROBLEMAS COMUNES:

   Error de conexión frontend:
   → Asegúrate que el backend esté corriendo en el puerto 8000

   Error al instalar dependencias:
   → pip install --upgrade pip
   → pip install -r requirements.txt

   Base de datos no se crea:
   → Verifica permisos de escritura en la carpeta

📖 MÁS INFO:

   Lee el archivo README.md para documentación completa

🎉 ¡LISTO PARA USAR!
   
   Base de datos ya inicializada con 20 productos:
   • Lácteos, Bebidas, Conservas, Panadería
   • Frutas y Verduras, Snacks, y más...

═══════════════════════════════════════════════════════════════

💡 TIP: Usa Ctrl+C para detener el servidor backend
